package jp.xhw.datapackupload.plugin.discord.panel.state;

public enum PanelStateType {

    PRE_DOWNLOAD,
    OVERWRITE_CHECK,
    ENABLE_CHECK,
    DOWNLOAD,
    INSTALL,
    ROLLBACK,
    DONE,
    SIZE_ERROR,
    VALIDATION_ERROR,
    CANCEL

}
